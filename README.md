# Neural-Network-Project
Built a multi-layer perceptron neural network from scratch in Python to classify data from the MAGIC Gamma Telescope dataset. Implemented forward propagation, backpropagation, gradient descent, and hyperparameter tuning while analyzing the effects of hidden layer size and learning rate on model performance.
